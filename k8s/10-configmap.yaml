apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: book-tracker
  labels:
    app.kubernetes.io/name: book-tracker
    app.kubernetes.io/component: backend
data:
  PORT: "5000"
  NODE_ENV: "production"
  MONGO_HOST: "mongo"
  MONGO_PORT: "27017"
  MONGO_DB: "booktracker"